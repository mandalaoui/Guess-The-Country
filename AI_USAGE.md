# AI Usage

---

## 1. AI Tools Used

- Cursor (AI-assisted code editor)
- ChatGPT (used for explanations, prompt refinement, and code review)

---

## 2. How AI Was Used

The project structure, file organization, and the entire UI layout—including how files are arranged, how code is separated, and how the page should look—were designed and specified manually.

AI tools were only used to assist with writing code inside the files according to my specifications. This included tasks such as:

- Generating starter datasets based on my chosen schema
- Writing functions and scaffolding code in backend service, controller, and route files as described by my requirements
- Producing the HTML, CSS, and JavaScript code to match the manual UI design and structure I defined
- Suggesting sample API requests and basic test cases

All AI-generated code and suggestions were thoroughly reviewed, revised, and integrated by me to fit the project’s structure.

---

## 3. Example Prompts

### Dataset Generation

Prompt:

Create a small dataset for the game.
Each country should have this structure:

{
  name: "Country Name",
  clues: [
    "clue 1",
    "clue 2",
    "clue 3"
  ]
}

Generate 10 countries with simple clues.
Export the dataset so it can be imported by other modules.

---

### Services

Prompt:

Create `services/gameService.js`.

Responsibilities:
1. Select a country from the dataset
2. Return only the clues
3. Keep track of the current country in memory
4. Validate a user's guess

Functions I expect:
getCountryClues()
checkGuess(userGuess)

The service should import the dataset from countries.js.

### Frontend UI

Prompt:

Create the file index.html for a very simple "Guess The Country" web application.

Context:
This assignment requires a minimal UI where a user sees three clues about a country, enters a guess, and submits it.

Requirements for the HTML page:
- Use plain HTML only.
- No frameworks or libraries.
- Link to styles.css.
- Load app.js as a module script.

The page should include:
1. A main title: "Guess The Country"
2. A section that displays exactly three clues
3. A text input where the user can type a country name
4. A submit button
5. An area where the result message will appear

Suggested structure:
- Header with title
- Clues section with an unordered list
- Guess form with input and button
- Result message container

These prompts represent the main types of interactions used during development.

## 4. Code Initially Generated by AI

AI generated initial drafts for several files, including:

- `backend/data/countries.js`
- `backend/services/gameService.js`
- `backend/controllers/gameController.js`
- `backend/routes/gameRoutes.js`
- `backend/server.js`
- `frontend/index.html`
- `frontend/styles.css`
- `frontend/api.js`
- `frontend/app.js`

These files were used as a starting point for the implementation.

---

## 5. What I Modified Manually

After the initial code generation, the following work was done manually:

- reviewed and edited the generated code
- connected the backend and frontend logic
- improved error handling
- adjusted code structure and naming
- fixed issues in the service/controller communication
- added the `getCurrentCountry()` function to properly return the correct answer
- refined the UI behavior

---

## 6. AI Configuration

No AI development configuration tools or rule files were used in this project.
---

## 7. Notes

AI was used as a development assistant to speed up scaffolding.  
All generated code was reviewed, tested, and modified manually to ensure it met the assignment requirements.